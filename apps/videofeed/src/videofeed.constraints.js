export const CHUNK_SIZE = 6;

const ABOVE = 1;
const ACTIVE = 1;
const BELOW = 2;
const TOTAL = ABOVE + ACTIVE + BELOW;

export const VIDEO_PLAYERS_TO_SHOW = {
  ABOVE,
  ACTIVE,
  BELOW,
  TOTAL,
};
